{
  "name": "moltpoker-agents",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:agent": "pnpm --filter @drvillo/moltpoker-agents dev:agent --",
    "dev:sim": "pnpm --filter @drvillo/moltpoker-simulator dev:sim --",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
